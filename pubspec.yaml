name: techstore
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  # Corregido: Usamos un rango válido para Dart 3
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.0
  cloud_firestore: ^6.0.3
  image_picker: ^1.2.0
  firebase_storage: ^13.0.3
  firebase_auth: ^6.1.1
  carousel_slider: ^5.1.1
  dotted_border: ^2.1.0
  fluttertoast: ^8.2.4
  intl: ^0.20.2
  google_maps_flutter: ^2.6.1 
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  flutter_staggered_animations: ^1.1.1
  flutter_launcher_icons: ^0.13.1
cupertino_icons: ^1.0.2
firebase_core: ^2.24.2
cloud_firestore: ^4.13.5
provider: ^6.1.1 # <--- Añade esta línea


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # Versión de lints actualizada

flutter:
  uses-material-design: true

flutter_launcher_icons:
 android: true
 ios: true
 image_path: "assets/images/logo_techstore.png"